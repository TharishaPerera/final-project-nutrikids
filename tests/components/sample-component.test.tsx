import { render, screen } from '@testing-library/react';
import { it, expect, describe } from 'vitest';

describe("Sample component testing group", () => {
  it("should pass the component testing", () => {
    expect(1).toBeTruthy();
  });
});


