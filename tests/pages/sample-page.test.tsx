import { render, screen } from '@testing-library/react';
import { it, expect, describe } from 'vitest'

describe('Sample pages testing group', () => {
    it('should pass the page testing', () => {
        expect(1).toBeTruthy();
    })
})